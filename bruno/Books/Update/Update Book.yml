info:
  name: Update Book
  type: http
  seq: 2

http:
  method: PATCH
  url: "{{Url}}/books/{{bookId}}"
  headers:
    - name: Authorization
      value: "{{jwt}}"
  body:
    type: json
    data: |2-
        {
          "description": "Updated desc"
        }

runtime:
  scripts:
    - type: tests
      code: |-
        test("should update book", () => {
          const data = res.getBody();
          expect(res.getStatus()).to.equal(200);
          expect(data.description).to.not.equal(bru.getEnvVar("bookDescription"));
        })

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
