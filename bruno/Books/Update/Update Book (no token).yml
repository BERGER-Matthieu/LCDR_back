info:
  name: Update Book (no token)
  type: http
  seq: 1

http:
  method: PATCH
  url: "{{Url}}/books/{{bookId}}"
  body:
    type: json
    data: |2-
        {
          "description": "Updated desc"
        }

runtime:
  scripts:
    - type: tests
      code: |-
        test("should NOT update book", () => {
          expect(res.getStatus()).to.equal(422);
        })

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
