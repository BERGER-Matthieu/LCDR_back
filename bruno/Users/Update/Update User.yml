info:
  name: Update User
  type: http
  seq: 2

http:
  method: PATCH
  url: "{{Url}}/users"
  headers:
    - name: Authorization
      value: "{{jwt}}"
  body:
    type: json
    data: |2-
        {
          "description": "Updated user description"
        }

runtime:
  scripts:
    - type: tests
      code: |-
        test("should update user", () => {
          const data = res.getBody();
          expect(res.getStatus()).to.equal(200);
          expect(data.description).to.not.equal(bru.getEnvVar("userDescription"));
        })

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
