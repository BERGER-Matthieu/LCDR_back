info:
  name: Delete User
  type: http
  seq: 2

http:
  method: DELETE
  url: "{{Url}}/users"
  headers:
    - name: Authorization
      value: "{{jwt}}"

runtime:
  scripts:
    - type: tests
      code: |-
        test("should delete user", () => {
          expect(res.getStatus()).to.equal(200);
        })

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
