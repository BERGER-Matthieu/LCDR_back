info:
  name: Get Post
  type: http
  seq: 2

http:
  method: GET
  url: "{{Url}}/posts/"
  params:
    - name: id
      value: 6a57649b1a06cbd938c28b29
      type: query
      disabled: true
    - name: skip
      value: "2"
      type: query
      disabled: true
    - name: limit
      value: "100"
      type: query
      disabled: true
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("should get all posts", () => {
          expect(res.getStatus()).to.equal(200);
        })

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
