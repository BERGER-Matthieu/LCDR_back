info:
  name: Get Post (specific)
  type: http
  seq: 1

http:
  method: GET
  url: "{{Url}}/posts/?id={{postId}}"
  params:
    - name: id
      value: "{{postId}}"
      type: query
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("should get new post", () => {
          expect(res.getStatus()).to.equal(200);
        })

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
