info:
  name: Delete Post
  type: http
  seq: 1

http:
  method: DELETE
  url: "{{Url}}/posts/{{postId}}"
  headers:
    - name: Authorization
      value: "{{jwt}}"

runtime:
  scripts:
    - type: tests
      code: |-
        test("should delete post", () => {
          expect(res.getStatus()).to.equal(200);
        })

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
