info:
  name: Get Comment (specific)
  type: http
  seq: 1

http:
  method: GET
  url: "{{Url}}/comments/"
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("should get all comment", () => {
          expect(res.getStatus()).to.equal(200);
        })

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
