info:
  name: Get Comment
  type: http
  seq: 2

http:
  method: GET
  url: "{{Url}}/comments/?postId={{postId}}"
  params:
    - name: postId
      value: "{{postId}}"
      type: query
    - name: skip
      value: "2"
      type: query
      disabled: true
    - name: limit
      value: "100"
      type: query
      disabled: true
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("should get new comment", () => {
          expect(res.getStatus()).to.equal(200);
        })

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
