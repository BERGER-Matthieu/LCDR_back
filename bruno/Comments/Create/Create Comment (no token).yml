info:
  name: Create Comment (no token)
  type: http
  seq: 1

http:
  method: POST
  url: "{{Url}}/comments/"
  body:
    type: json
    data: |-
      {
        "content": "{{commentContent}}",
        "postId": "{{postId}}"
      }
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("should NOT create new comment", () => {
          expect(res.getStatus()).to.equal(422);
        })

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
