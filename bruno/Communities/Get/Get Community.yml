info:
  name: Get Community
  type: http
  seq: 2

http:
  method: GET
  url: "{{Url}}/communities/"
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("should get communities", () => {
          expect(res.getStatus()).to.equal(200);
        })

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
