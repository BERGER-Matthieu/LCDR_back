info:
  name: Delete Community (no token)
  type: http
  seq: 1

http:
  method: DELETE
  url: "{{Url}}/communities/{{communityId}}"

runtime:
  scripts:
    - type: tests
      code: |-
        test("should delete community", () => {
          expect(res.getStatus()).to.equal(422);
        })

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
